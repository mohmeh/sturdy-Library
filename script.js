const library = [];

function CreateBook (author, title, pages){ 
  var book = {
  this.author = author;
  this.title = title;
  this.pages = pages;
  const bookId = crypto.randomUUID(this.book);
};
 console.log(book);
  
library.push(CreateBook); 
};

function displayLibrary (){
for (const book of library){
  console.log(library);
}
  displayLibrary();
}
function onClick(){
  return CreateBook;
},
var newBook = document.createElement('button');
newBook.innerText = "Add new Book";
newBook.addEventListener('click', onClick);
document.body.appendChild(newbook);
